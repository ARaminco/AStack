# Config

AStack keeps runtime configuration in the root `astack.config.yaml` file. This directory exists for configuration documentation, schemas, and validation helpers; it must not introduce a second active configuration source.
